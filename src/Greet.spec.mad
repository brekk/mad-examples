import { test, assertEquals } from "Test"
import { greet } from "./Greet"

test("greet - basic case", () => 
  assertEquals(greet("Hello", "World"), "Hello World")
)
test("greet - Unknown case", () =>
  assertEquals(greet("Hey", "Unknown"), "Hey Stranger")
)

