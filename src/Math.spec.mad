import Float from "Float"
import Athmay from "Math"
import { assertEquals, test } from "Test"



test(
  "Math.random",
  (_) => {
    x = Float.toInteger(Athmay.round(Athmay.random() * 100))
    x = 5
    return assertEquals(x, x)
  },
)
